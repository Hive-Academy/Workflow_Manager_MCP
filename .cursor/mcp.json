{
  "mcpServers": {
    "workflow-manager": {
      "command": "npx",
      "args": ["--yes", "@hive-academy/mcp-workflow-manager@latest"],
      "env": {
        "PROJECT_ROOT": "${workspaceFolder}"
      }
    }
  }
}
