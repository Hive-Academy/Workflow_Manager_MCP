<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delegation Analytics - Workflow Report</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', system-ui, sans-serif; }
        .chart-container { position: relative; height: 400px; margin: 1rem 0; }
        .metric-card { @apply bg-white rounded-lg shadow-sm border border-gray-200 p-6 hover:shadow-md transition-shadow duration-200; }
        .status-badge { @apply inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium border; }
        .priority-badge { @apply inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium; }
        .print-hidden { @media print { display: none !important; } }
        @media print { body { @apply text-sm; } .chart-container { height: 300px; } }
    </style>
</head>
<body class="bg-gray-50">
    <div class="min-h-screen bg-gradient-to-br from-slate-50 to-purple-50 py-8">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            
            <!-- Enhanced Header -->
            <div class="relative bg-gradient-to-br from-purple-600 via-indigo-600 to-blue-700 rounded-2xl shadow-xl p-8 mb-8 overflow-hidden">
                <!-- Background Pattern -->
                <div class="absolute inset-0 bg-white/10 backdrop-blur-sm"></div>
                <div class="absolute top-0 right-0 w-96 h-96 bg-white/5 rounded-full -translate-y-48 translate-x-48"></div>
                <div class="absolute bottom-0 left-0 w-64 h-64 bg-white/5 rounded-full translate-y-32 -translate-x-32"></div>
                
                <!-- Content -->
                <div class="relative z-10">
                    <div class="flex items-center justify-between mb-6">
                        <div>
                            <div class="flex items-center space-x-3 mb-3">
                                <div class="bg-white/20 p-3 rounded-xl backdrop-blur-sm">
                                    <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16l-4-4m0 0l4-4m-4 4h18"></path>
                                    </svg>
                                </div>
                                <div>
                                    <h1 class="text-4xl font-bold text-white mb-1">Delegation Analytics</h1>
                                    <p class="text-purple-100 text-lg">Delegation patterns, handoff efficiency, and role performance analysis</p>
                                </div>
                            </div>
                            <p class="text-purple-200">Generated on {{formatDate generatedAt "MMMM Do, YYYY [at] h:mm A"}}</p>
                        </div>
                        <div class="flex flex-col items-end space-y-3">
                            <div class="bg-white/20 backdrop-blur-sm text-white px-6 py-3 rounded-xl font-medium border border-white/30">
                                <div class="text-sm text-purple-100 mb-1">Report Period</div>
                                <div class="text-lg font-semibold">{{dateRange.start}} - {{dateRange.end}}</div>
                            </div>
                            {{#if filters}}
                            <div class="bg-white/15 backdrop-blur-sm text-white px-4 py-2 rounded-lg text-sm border border-white/20">
                                <div class="text-xs text-purple-100 mb-1">Active Filters</div>
                                <div>{{#each filters}}{{@key}}: {{this}}{{#unless @last}}, {{/unless}}{{/each}}</div>
                            </div>
                            {{/if}}
                        </div>
                    </div>
                </div>
            </div>

            <!-- Delegation Flow Visualization -->
            <div class="bg-white rounded-xl shadow-lg border border-gray-200 p-8 mb-8">
                <h3 class="text-xl font-semibold text-gray-900 mb-6 flex items-center">
                    <span class="text-2xl mr-3">🌊</span>
                    Delegation Flow Visualization
                </h3>
                <div class="overflow-x-auto">
                    <div class="flex justify-between items-center bg-gray-50 rounded-xl p-8 my-6 overflow-x-auto" style="min-width: 800px;">
                        <div class="text-center p-6 rounded-xl min-w-[120px] relative transition-transform duration-300 hover:scale-105 bg-gradient-to-br from-blue-500 to-blue-700 text-white">
                            <div class="font-semibold text-sm mb-2 uppercase tracking-wide">🪃 Boomerang</div>
                            <div class="text-2xl font-bold mb-2">{{defaultValue metrics.delegations.roleStats.boomerang 0}}</div>
                            <div class="text-xs opacity-90">
                                {{defaultValue (multiply (defaultValue metrics.delegations.roleEfficiency.boomerang 0) 100) 0}}% efficiency
                            </div>
                        </div>
                        <div class="text-2xl text-gray-400 mx-4 animate-pulse">→</div>
                        <div class="text-center p-6 rounded-xl min-w-[120px] relative transition-transform duration-300 hover:scale-105 bg-gradient-to-br from-green-500 to-green-700 text-white">
                            <div class="font-semibold text-sm mb-2 uppercase tracking-wide">🔬 Researcher</div>
                            <div class="text-2xl font-bold mb-2">{{defaultValue metrics.delegations.roleStats.researcher 0}}</div>
                            <div class="text-xs opacity-90">
                                {{defaultValue (multiply (defaultValue metrics.delegations.roleEfficiency.researcher 0) 100) 0}}% efficiency
                            </div>
                        </div>
                        <div class="text-2xl text-gray-400 mx-4 animate-pulse">→</div>
                        <div class="text-center p-6 rounded-xl min-w-[120px] relative transition-transform duration-300 hover:scale-105 bg-gradient-to-br from-yellow-500 to-yellow-700 text-white">
                            <div class="font-semibold text-sm mb-2 uppercase tracking-wide">🏛️ Architect</div>
                            <div class="text-2xl font-bold mb-2">{{defaultValue metrics.delegations.roleStats.architect 0}}</div>
                            <div class="text-xs opacity-90">
                                {{defaultValue (multiply (defaultValue metrics.delegations.roleEfficiency.architect 0) 100) 0}}% efficiency
                            </div>
                        </div>
                        <div class="text-2xl text-gray-400 mx-4 animate-pulse">→</div>
                        <div class="text-center p-6 rounded-xl min-w-[120px] relative transition-transform duration-300 hover:scale-105 bg-gradient-to-br from-purple-500 to-purple-700 text-white">
                            <div class="font-semibold text-sm mb-2 uppercase tracking-wide">👨‍💻 Developer</div>
                            <div class="text-2xl font-bold mb-2">{{defaultValue metrics.delegations.roleStats.senior-developer 0}}</div>
                            <div class="text-xs opacity-90">
                                {{defaultValue (multiply (defaultValue metrics.delegations.roleEfficiency.senior-developer 0) 100) 0}}% efficiency
                            </div>
                        </div>
                        <div class="text-2xl text-gray-400 mx-4 animate-pulse">→</div>
                        <div class="text-center p-6 rounded-xl min-w-[120px] relative transition-transform duration-300 hover:scale-105 bg-gradient-to-br from-red-500 to-red-700 text-white">
                            <div class="font-semibold text-sm mb-2 uppercase tracking-wide">🔍 Code Review</div>
                            <div class="text-2xl font-bold mb-2">{{defaultValue metrics.delegations.roleStats.code-review 0}}</div>
                            <div class="text-xs opacity-90">
                                {{defaultValue (multiply (defaultValue metrics.delegations.roleEfficiency.code-review 0) 100) 0}}% efficiency
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Delegation Metrics -->
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-8">
                <div class="bg-white rounded-xl shadow-lg border border-gray-200 p-6 transition-all duration-300 hover:shadow-xl">
                    <div class="flex justify-between items-center mb-4">
                        <div class="text-xl">🎯</div>
                        <div class="text-xs px-2 py-1 rounded font-semibold bg-green-100 text-green-600">↗ +12%</div>
                    </div>
                    <div class="text-3xl font-bold mb-2 text-green-600">{{defaultValue metrics.delegations.successRate 0}}%</div>
                    <div class="text-gray-600 text-sm font-medium">Delegation Success Rate</div>
                </div>

                <div class="bg-white rounded-xl shadow-lg border border-gray-200 p-6 transition-all duration-300 hover:shadow-xl">
                    <div class="flex justify-between items-center mb-4">
                        <div class="text-xl">⚡</div>
                        <div class="text-xs px-2 py-1 rounded font-semibold bg-red-100 text-red-600">↘ -8%</div>
                    </div>
                    <div class="text-3xl font-bold mb-2 text-blue-600">{{defaultValue metrics.delegations.avgHandoffTime 0}}h</div>
                    <div class="text-gray-600 text-sm font-medium">Avg Handoff Time</div>
                </div>

                <div class="bg-white rounded-xl shadow-lg border border-gray-200 p-6 transition-all duration-300 hover:shadow-xl">
                    <div class="flex justify-between items-center mb-4">
                        <div class="text-xl">🔄</div>
                        <div class="text-xs px-2 py-1 rounded font-semibold bg-gray-100 text-gray-600">→ Stable</div>
                    </div>
                    <div class="text-3xl font-bold mb-2 text-yellow-600">{{defaultValue metrics.delegations.avgRedelegationCount 0}}</div>
                    <div class="text-gray-600 text-sm font-medium">Avg Redelegations</div>
                </div>

                <div class="bg-white rounded-xl shadow-lg border border-gray-200 p-6 transition-all duration-300 hover:shadow-xl">
                    <div class="flex justify-between items-center mb-4">
                        <div class="text-xl">📊</div>
                        <div class="text-xs px-2 py-1 rounded font-semibold bg-green-100 text-green-600">↗ +5%</div>
                    </div>
                    <div class="text-3xl font-bold mb-2 text-blue-600">{{defaultValue metrics.delegations.totalDelegations 0}}</div>
                    <div class="text-gray-600 text-sm font-medium">Total Delegations</div>
                </div>

                <div class="bg-white rounded-xl shadow-lg border border-gray-200 p-6 transition-all duration-300 hover:shadow-xl">
                    <div class="flex justify-between items-center mb-4">
                        <div class="text-xl">⏱️</div>
                        <div class="text-xs px-2 py-1 rounded font-semibold bg-red-100 text-red-600">↘ -15%</div>
                    </div>
                    <div class="text-3xl font-bold mb-2 text-green-600">{{defaultValue metrics.delegations.avgCompletionTime 0}}h</div>
                    <div class="text-gray-600 text-sm font-medium">Avg Role Completion Time</div>
                </div>

                <div class="bg-white rounded-xl shadow-lg border border-gray-200 p-6 transition-all duration-300 hover:shadow-xl">
                    <div class="flex justify-between items-center mb-4">
                        <div class="text-xl">🏆</div>
                        <div class="text-xs px-2 py-1 rounded font-semibold bg-green-100 text-green-600">↗ +18%</div>
                    </div>
                    <div class="text-3xl font-bold mb-2 text-green-600">{{defaultValue metrics.delegations.mostEfficientRole 'N/A'}}</div>
                    <div class="text-gray-600 text-sm font-medium">Most Efficient Role</div>
                </div>
            </div>

            <!-- Delegation Flow Chart -->
            <div class="bg-white rounded-xl shadow-lg border border-gray-200 p-6 mb-8">
                <h3 class="text-lg font-semibold text-gray-900 mb-4 flex items-center">
                    <span class="text-xl mr-2">📈</span>
                    Delegation Flow Trends
                </h3>
                <div class="relative h-80">
                    <canvas id="delegationFlowChart"></canvas>
                </div>
            </div>

            <!-- Role Efficiency Chart -->
            <div class="bg-white rounded-xl shadow-lg border border-gray-200 p-6 mb-8">
                <h3 class="text-lg font-semibold text-gray-900 mb-4 flex items-center">
                    <span class="text-xl mr-2">⚡</span>
                    Role Efficiency Comparison
                </h3>
                <div class="relative h-80">
                    <canvas id="roleEfficiencyChart"></canvas>
                </div>
            </div>

            <!-- Bottleneck Analysis -->
            {{#if metrics.delegations.bottlenecks}}
            {{#if (gt (length metrics.delegations.bottlenecks) 0)}}
            <div class="bg-gradient-to-br from-yellow-50 to-orange-50 border border-yellow-200 rounded-xl p-8 my-8">
                <div class="text-yellow-800 text-xl font-semibold mb-6 flex items-center gap-2">
                    ⚠️ Workflow Bottleneck Analysis
                </div>
                {{#each metrics.delegations.bottlenecks}}
                    <div class="bg-white rounded-lg p-6 mb-4 border-l-4 border-yellow-500 shadow-sm">
                        <div class="font-semibold text-gray-900 mb-2">{{this.role}}</div>
                        <div class="text-gray-700 mb-3 leading-relaxed">{{this.issue}}</div>
                        <div class="inline-block px-3 py-1 rounded text-xs font-semibold uppercase tracking-wide {{#if (eq this.impact 'high')}}bg-red-100 text-red-800{{else if (eq this.impact 'medium')}}bg-yellow-100 text-yellow-800{{else}}bg-green-100 text-green-800{{/if}}">
                            {{this.impact}} Impact
                        </div>
                    </div>
                {{/each}}
            </div>
            {{/if}}
            {{/if}}

            <!-- Role Transition Matrix -->
            <div class="bg-white rounded-xl shadow-lg border border-gray-200 p-8 my-8 overflow-x-auto">
                <div class="text-gray-900 text-xl font-semibold mb-6">🔀 Role Transition Matrix</div>
                <table class="w-full border-collapse min-w-[600px]">
                    <thead>
                        <tr>
                            <th class="p-4 text-center border border-gray-200 bg-gray-50 text-gray-900 font-semibold text-sm">From / To</th>
                            <th class="p-4 text-center border border-gray-200 bg-gray-50 text-gray-900 font-semibold text-sm">🪃 Boomerang</th>
                            <th class="p-4 text-center border border-gray-200 bg-gray-50 text-gray-900 font-semibold text-sm">🔬 Researcher</th>
                            <th class="p-4 text-center border border-gray-200 bg-gray-50 text-gray-900 font-semibold text-sm">🏛️ Architect</th>
                            <th class="p-4 text-center border border-gray-200 bg-gray-50 text-gray-900 font-semibold text-sm">👨‍💻 Developer</th>
                            <th class="p-4 text-center border border-gray-200 bg-gray-50 text-gray-900 font-semibold text-sm">🔍 Code Review</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each (array 'boomerang' 'researcher' 'architect' 'senior-developer' 'code-review')}}
                            <tr>
                                <td class="p-4 text-center border border-gray-200 bg-white text-gray-600"><strong>{{this}}</strong></td>
                                {{#each (array 'boomerang' 'researcher' 'architect' 'senior-developer' 'code-review')}}
                                    {{#with (lookup ../metrics.delegations.transitionMatrix ../this)}}
                                        {{#with (lookup this @this)}}
                                            <td class="p-4 text-center border border-gray-200 bg-white text-gray-600 font-semibold text-gray-900 {{#if (gt this 10)}}bg-green-100 text-green-800{{else}}{{#if (gt this 5)}}bg-yellow-100 text-yellow-800{{else}}bg-gray-100 text-gray-600{{/if}}{{/if}}">{{this}}</td>
                                        {{else}}
                                            <td class="p-4 text-center border border-gray-200 bg-white text-gray-600 font-semibold text-gray-900 bg-gray-100 text-gray-600">0</td>
                                        {{/with}}
                                    {{else}}
                                        <td class="p-4 text-center border border-gray-200 bg-white text-gray-600 font-semibold text-gray-900 bg-gray-100 text-gray-600">0</td>
                                    {{/with}}
                                {{/each}}
                            </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>

            <!-- Efficiency Indicators -->
            <div>
                <h3 class="text-xl font-semibold text-gray-900 mb-6 flex items-center">
                    <span class="text-2xl mr-3">📈</span>
                    Role Efficiency Indicators
                </h3>
                <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-6 my-8">
                    {{#each (array 'boomerang' 'researcher' 'architect' 'senior-developer' 'code-review')}}
                        {{#with (lookup ../metrics.delegations.roleEfficiency this)}}
                            {{#with (multiply this 100)}}
                                <div class="bg-white rounded-xl shadow-lg border border-gray-200 p-6 text-center transition-all duration-300 hover:shadow-xl">
                                    <div class="text-3xl font-bold mb-2 {{#if (gt this 80)}}text-green-600{{else}}{{#if (gt this 60)}}text-yellow-600{{else}}text-red-600{{/if}}{{/if}}">
                                        {{defaultValue this 0}}%
                                    </div>
                                    <div class="text-sm text-gray-600 font-medium">{{defaultValue (replace ../this '-' ' ') this}}</div>
                                </div>
                            {{/with}}
                        {{else}}
                            <div class="bg-white rounded-xl shadow-lg border border-gray-200 p-6 text-center transition-all duration-300 hover:shadow-xl">
                                <div class="text-3xl font-bold mb-2 text-gray-400">0.0%</div>
                                <div class="text-sm text-gray-600 font-medium">{{defaultValue (replace this '-' ' ') this}}</div>
                            </div>
                        {{/with}}
                    {{/each}}
                </div>
            </div>

            <!-- Key Insights -->
            <div class="bg-gradient-to-r from-purple-50 to-indigo-50 rounded-xl p-6 border border-purple-100 mt-8">
                <h3 class="text-xl font-bold text-gray-900 mb-4 flex items-center">
                    <span class="text-2xl mr-3">💡</span>
                    Delegation Insights & Recommendations
                </h3>
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                    {{#with metrics.delegations.successRate}}
                        {{#if (gt this 85)}}
                            <div class="bg-green-50 border border-green-200 rounded-lg p-6">
                                <div class="text-green-800 font-semibold mb-2">🎉 Excellent Delegation Flow</div>
                                <div class="text-gray-700 text-sm">
                                    High success rate of {{defaultValue this 0}}% indicates smooth role transitions and clear handoffs.
                                </div>
                            </div>
                        {{else if (gt this 70)}}
                            <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-6">
                                <div class="text-yellow-800 font-semibold mb-2">📈 Good Delegation Performance</div>
                                <div class="text-gray-700 text-sm">
                                    Success rate of {{defaultValue this 0}}% shows solid delegation patterns with room for optimization.
                                </div>
                            </div>
                        {{else}}
                            <div class="bg-red-50 border border-red-200 rounded-lg p-6">
                                <div class="text-red-800 font-semibold mb-2">⚠️ Delegation Issues Detected</div>
                                <div class="text-gray-700 text-sm">
                                    Low success rate of {{defaultValue this 0}}% suggests delegation bottlenecks need immediate attention.
                                </div>
                            </div>
                        {{/if}}
                    {{/with}}
                    
                    {{#with metrics.delegations.avgRedelegationCount}}
                        {{#if (lt this 1.5)}}
                            <div class="bg-green-50 border border-green-200 rounded-lg p-6">
                                <div class="text-green-800 font-semibold mb-2">✅ Minimal Redelegations</div>
                                <div class="text-gray-700 text-sm">
                                    Low redelegation rate of {{defaultValue this 0}} indicates clear requirements and effective handoffs.
                                </div>
                            </div>
                        {{else if (gt this 2.5)}}
                            <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-6">
                                <div class="text-yellow-800 font-semibold mb-2">🔄 High Redelegation Rate</div>
                                <div class="text-gray-700 text-sm">
                                    Average of {{defaultValue this 0}} redelegations suggests unclear requirements or quality issues.
                                </div>
                            </div>
                        {{/if}}
                    {{/with}}
                    
                    {{#if metrics.delegations.mostEfficientRole}}
                        <div class="bg-blue-50 border border-blue-200 rounded-lg p-6">
                            <div class="text-blue-800 font-semibold mb-2">🏆 Top Performing Role</div>
                            <div class="text-gray-700 text-sm">
                                {{defaultValue (replace metrics.delegations.mostEfficientRole '-' ' ') metrics.delegations.mostEfficientRole}} shows highest efficiency - consider analyzing their practices for workflow optimization.
                            </div>
                        </div>
                    {{/if}}
                </div>
            </div>
        </div>
    </div>

    <script>
    document.addEventListener('DOMContentLoaded', function() {
        // Delegation Flow Trends Chart
        const delegationFlowCtx = document.getElementById('delegationFlowChart').getContext('2d');
        new Chart(delegationFlowCtx, {
            type: 'line',
            data: {
                labels: ['Week 1', 'Week 2', 'Week 3', 'Week 4'],
                datasets: [{
                    label: 'Successful Delegations',
                    data: [{{defaultValue metrics.delegations.weeklyTrends.successful '12, 15, 18, 22'}},
                    borderColor: '#10b981',
                    backgroundColor: 'rgba(16, 185, 129, 0.1)',
                    tension: 0.4
                }, {
                    label: 'Failed Delegations',
                    data: [{{defaultValue metrics.delegations.weeklyTrends.failed '3, 2, 4, 1'}},
                    borderColor: '#ef4444',
                    backgroundColor: 'rgba(239, 68, 68, 0.1)',
                    tension: 0.4
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        labels: { color: '#374151' }
                    }
                },
                scales: {
                    x: { 
                        ticks: { color: '#6b7280' },
                        grid: { color: '#e5e7eb' }
                    },
                    y: { 
                        ticks: { color: '#6b7280' },
                        grid: { color: '#e5e7eb' }
                    }
                }
            }
        });

        // Role Efficiency Chart
        const roleEfficiencyCtx = document.getElementById('roleEfficiencyChart').getContext('2d');
        new Chart(roleEfficiencyCtx, {
            type: 'radar',
            data: {
                labels: ['Boomerang', 'Researcher', 'Architect', 'Developer', 'Code Review'],
                datasets: [{
                    label: 'Efficiency %',
                    data: [
                        {{multiply (defaultValue metrics.delegations.roleEfficiency.boomerang 0) 100}},
                        {{multiply (defaultValue metrics.delegations.roleEfficiency.researcher 0) 100}},
                        {{multiply (defaultValue metrics.delegations.roleEfficiency.architect 0) 100}},
                        {{multiply (defaultValue metrics.delegations.roleEfficiency.senior-developer 0) 100}},
                        {{multiply (defaultValue metrics.delegations.roleEfficiency.code-review 0) 100}}
                    ],
                    borderColor: '#3b82f6',
                    backgroundColor: 'rgba(59, 130, 246, 0.1)',
                    pointBackgroundColor: '#3b82f6',
                    pointBorderColor: '#1d4ed8',
                    pointHoverBackgroundColor: '#1d4ed8',
                    pointHoverBorderColor: '#3b82f6'
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        labels: { color: '#374151' }
                    }
                },
                scales: {
                    r: {
                        angleLines: { color: '#e5e7eb' },
                        grid: { color: '#e5e7eb' },
                        pointLabels: { color: '#6b7280' },
                        ticks: { 
                            color: '#6b7280',
                            backdropColor: 'transparent'
                        },
                        min: 0,
                        max: 100
                    }
                }
            }
        });
    });
    </script>
</body>
</html>